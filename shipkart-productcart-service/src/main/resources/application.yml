spring:
  application:
    name: product-cart
  config:
    import: configserver:http://localhost:8888

server:
  port: 8085
management:
  endpoints:
    web:
     exposure:
       include: '*'
  endpoint:
    health:
      show-details: always


